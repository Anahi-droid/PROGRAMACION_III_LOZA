const estudiante = "Lucía";

const nivel = 2;

const progreso = 85.5;

let leccionesCompletadas = 0;

leccionesCompletadas = leccionesCompletadas + 1;

leccionesCompletadas++;

console.log(`${estudiante} está en el nivel ${nivel}`);

var idiomaAntiguo = "Latín";